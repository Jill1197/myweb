# myweb

##Step1. Clone Repository

Step2. Create .env Copy from .env.example

Step3. Generate password

Step4. Create file access.log and essential-access.log
```sh
touch access.log && touch essential-access.log
```

Step5. Deploy Docker
```sh
docker compose up -d
```
